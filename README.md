# README

## External Libraries
- nltk
- textblob
- spacy
- numpy
- pandas
- matplotlib

## Source Code: `programming_assignment_1.ipynb`

## Input File: `cantrbry/alice29.txt`

## Output Folder: `output/`
The output files, `cleaned.txt`, `words.txt`, and `top10words.txt`, for each framework, `nltk`, `textblob`, and `spacy` are saved in `output/nltk`, `output/textblob`, and `output/spacy` respectively. The time comparison for the frameworks is saved in `output/time_compares.txt`. 
